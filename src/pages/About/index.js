import React, {Component} from 'react';
import PropTypes from 'prop-types';

export default class About extends Component {
    render() {
        // console.log('About 收到props:',this.props)

        return (
                 <h3>我是About的内容</h3>
         );
    }
}
